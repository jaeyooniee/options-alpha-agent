# Hackathon Compliance Matrix

| Requirement | Repository evidence | Status |
|---|---|---|
| Autonomous AI trading agent | Featherless OpenAI-compatible provider boundary; deterministic look-ahead-safe daily regime plus a one-minute pullback/reversal signal, strict JSON schema, fail-closed decisions, market-clock and entry-window gates, 2--10 DTE short-horizon candidate policy, exit-first entry block, tested exact-leg inverse close preview, daily call/cost guards, hash-chained AI audit log, option-candidate selector, `run_shadow_cycle`, one-shot per-minute `shadow-cycle` worker, and sanitized `docs/live-shadow-evidence.md` | In progress; closed-market live path verified, scheduled deployment, broker close submission, and open-market AI shadow run pending |
| Alpaca Trading API | Sanitized account/freshness probe, free option-data probe, `capture-bars`, and immutable `capture-option-snapshot` commands using official SDK clients | Verified read-only |
| Alpaca MCP or CLI | Installed Alpaca Codex plugin called for chain, quote, IV, and Greeks; two matching 22-contract snapshots, strict validator, and exact-symbol quote-path comparison; `docs/alpaca-mcp-evidence.md` and `docs/option-snapshot-provenance.md` | Verified read-only |
| AI provider integration | Featherless OpenAI-compatible API, selected model, read-only plan/model probe, synthetic `ai-smoke` contract validation, and `docs/featherless-provider-evidence.md` | Verified metadata + synthetic smoke; live-market inference pending |
| Options in every strategy | Risk allowlist contains options strategies only | Implemented |
| Paper trading | Configuration rejects live mode; every source `TradingClient` construction is statically regression-tested as literal `paper=True`; two independent paper-execution flags plus a normally-on kill switch are required | Implemented |
| Fresh $100,000 account | ACTIVE; $100,000; zero orders/fills/positions; Level 3; raw ID redacted | Verified 2026-08-29 |
| Risk gates | Deterministic pre-trade policy, AI-independent exit policy, order lifecycle policy, and tests | Implemented |
| AI decision auditability | `src/options_alpha_agent/ai.py`, execution events, read-only per-order reconciliation, `shadow_performance.py`, and `short_shadow.py`; local and GCS audit append paths reject credential-like fields/text before hashing or persistence, while preserving provider/model metadata, sanitized evidence, decision, hashes, usage, idempotent client ID, hash-redacted broker reference, fill quantity/average price/lifecycle state, position-level P&L, and conservative exact-leg shadow P&L | Implemented for decision/shadow boundary; real paper-fill and position-to-order P&L attribution pending |
| One-page write-up | Source at `docs/one-page-writeup.md`; polished A4 one-page PDF at `output/pdf/options-alpha-one-page.pdf` | Local artifact complete |
| Public GitHub repository | Public repository at https://github.com/jaeyooniee/options-alpha-agent; `main` branch; passing GitHub Actions run at https://github.com/jaeyooniee/options-alpha-agent/actions/runs/33583068872 | Verified 2026-09-02; future pushes remain approval-gated |
| Demo application | Dependency-free offline end-to-end demo, read-only local dashboard, static public-safe judge overview at `/demo`, `/api/healthz`, and account/P&L reconciliation command; public working URL still requires approved deployment | In progress |
| Video and slides | Demo script, visually checked local PPTX/PDF, `submission/cover.png` (1600x900), and decoded narrated H.264/AAC 1080p `submission/demo.mp4`; deterministic build and frame-QA scripts included | Local required artifacts complete; live-dashboard screen capture remains an optional upgrade |
| Reproducible evaluation | Seeded four-strategy simulator; multi-regime robustness sweep; SPY/QQQ underlying holdouts; two hashed real option snapshots plus exact-symbol conservative quote-path comparison; normalized option replay with stale/adverse-fill stress; real-source-only 1/5/15-minute exact-leg shadow cohort evaluator | In progress; multi-date option history, signal/OI provenance, and broker-fill reconciliation pending |
| Working cloud agent | Cloud Run Job design, fail-closed worker/dashboard images, GCS generation-guarded audit backend, time-bounded distributed GCS run lock, read-only per-order/fill reconciliation, `cloud-preflight`, and CI worker/demo plus dashboard `/api/healthz`/`/demo` smoke-test configuration | Pre-deployment implemented and tested; cloud resource creation requires approval |
| MIT compliance | Root `LICENSE` | Implemented |
